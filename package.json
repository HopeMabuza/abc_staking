{
  "name": "my-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "pnpm --filter contracts compile",
    "test": "pnpm --filter contracts test",
    "dev": "pnpm --filter frontend dev",
    "node": "pnpm --filter contracts node",
    "deploy:local": "pnpm --filter contracts deploy:local"
  },
  "devDependencies": {}
}
